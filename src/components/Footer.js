import React from 'react';
import {footer} from "../stub";

const Footer = () => {
    return (
        <footer>
            <p>{footer.content}</p>
        </footer>
    );
};

export default Footer;